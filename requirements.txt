dataclasses_json==0.6.4
shutup==0.2.0
humanize
pandas
numpy
torch
pyyaml
pydantic
python-dotenv

# Cloud LLM backend (default, see src/llm/backend.py). Requires the
# OPENAI_API_KEY environment variable to be set.
openai

# Optional, only needed if you switch llm.backend to "llama_cpp" in
# configs/config.yaml to run a local GGUF model instead:
# llama-cpp-python==0.3.4  (install with --extra-index-url for your CUDA version)
